{
	"name": "@cypress-plugin-snapshots/test",
	"description": "Example setup for cypress-plugin-snapshots",
	"version": "1.1.0",
	"license": "MIT",
	"engine": {
		"node": ">=20"
	},
	"scripts": {
		"ci:test": "start-server-and-test start http://localhost:8080 test",
		"cy:open": "cypress open",
		"cy:run": "cypress run --browser chrome",
		"cy:verify": "cypress verify",
		"start": "node ./test-server/index.js",
		"test": "cypress run --browser chrome"
	},
	"dependencies": {
		"@cypress-plugin-snapshots/lib": "workspace:*",
		"cypress": "^10.0.0",
		"express": "^4.0.0",
		"start-server-and-test": "^1.0.0"
	}
}
