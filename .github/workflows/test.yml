name: Test

on: push

jobs:
  unit_test:
    name: Unit Test
    runs-on: ubuntu-latest
    container: cypress/browsers:latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          cache: yarn
          node-version: 20
      - name: Install
        run: yarn --immutable
      - name: Test
        run: yarn test:unit

  e2e_test:
    name: E2E Test
    runs-on: ubuntu-latest
    container: cypress/browsers:latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          cache: yarn
          node-version: 20
      - name: Install
        run: yarn --immutable
      - name: Test
        run: |
          echo $PWD
          chmod o+w .
          ls -la .
          yarn test:e2e
          kill $(jobs -p) || true
        working-directory: ./packages/test
      - name: Archive Cypress snapshots
        uses: actions/upload-artifact@v1
        if: failure()
        with:
          name: cypress-snapshots
          path: packages/test/cypress/integration/__*snapshots__/*
